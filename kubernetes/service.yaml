apiVersion: v1
kind: Service
metadata:
  name: linkedin-mcp
  namespace: linkedin-mcp
  labels:
    app.kubernetes.io/name: linkedin-mcp
spec:
  selector:
    app: linkedin-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
